<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!--Establezca los permisos para aplicarlos a elementos en las vistas con el parámetro groups-->

        <record id="edit_sent_payment_date" model="res.groups">
            <field name="name">Acceso a fechas de envío de facturación en tareas (Escritura)</field>
            <field name="users" eval="[(4, ref('base.user_root'))]"/>
        </record>

        <!--Establezca los permisos para aplicarlos a elementos en las vistas con el parámetro groups-->
        <record id="view_datos_sent_payment_date" model="res.groups">
            <field name="name">Acceso a fechas de envío de facturación en tareas (Lectura)</field>
            <field name="users" eval="[(4, ref('base.user_root'))]"/>
        </record>

    </data>
</odoo>